import 'package:flutter/material.dart';

class SongBottomWidget extends StatelessWidget {
  const SongBottomWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}